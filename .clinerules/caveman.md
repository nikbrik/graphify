Read `.agents/integrations/caveman.md`.

Use repo-local caveman skills from `.agents/skills/` when relevant. For
Russian-language prompts, answer in Russian with terse technical prose while
preserving code, paths, commands, URLs, identifiers, and exact errors. Do not
run broad upstream installers or write global Claude config unless explicitly
asked.
